spring:
  data:
    mongodb:
      uri: mongodb://${MONGODB_HOST}:${MONGODB_PORT}/book-db
#      uri: mongodb://localhost:27017/book-db
  rabbitmq:
    host: ${RABBITMQ_HOST}
    port: ${RABBITMQ_PORT}
    password: guest
    username: guest
    virtual-host: /


grpc:
  server:
    port: ${GRPC_SERVER_PORT}

rabbitmq:
  queues:
    student-created: bookCreated
    student-updated: bookUpdated
    student-deleted: bookDeleted

server:
  port: 8082
